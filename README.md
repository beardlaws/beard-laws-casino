# Beard Laws Casino 2.0.3 — PixiJS 8 Loader Fix

Fixes `Texture.fromURL is not a function` by using PixiJS 8 `Assets.load()`.

Upload all contents to the repository root and overwrite matching files. You do not need to delete the repository again.
